namespace Ohtic.Test.Data.Models.Indexes
{
    public record struct CustomerId(int Value);
    public record struct CustomerEmail(string Value);
    public record struct WeatherForecastId(int Value);
}
